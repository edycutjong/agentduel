# secrets — never commit
.env
.env.local
.env.*.local
!.env.example

# deps
node_modules/
web/node_modules/

# build output
dist/
web/.next/
web/out/

# sqlite runtime db (regenerated by settle / seed)
db/*.sqlite
db/*.sqlite-shm
db/*.sqlite-wal
*.sqlite

# logs / misc
*.log
.DS_Store
.vitest/
coverage/

# internal build scaffolding (not for public repo)
_BUILD_BRIEF.md

# Playwright (E2E) — NOTE: assets/ is committed brand work, do NOT ignore it
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Lighthouse CI
.lighthouseci/
